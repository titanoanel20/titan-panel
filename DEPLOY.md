# 🚀 راهنمای کامل دیپلوی TiTaN

پنل TiTaN یک پروژه **تکسرویس** است: پایتون (پنل) + Nginx (پروکسی جلویی) + Xray-core (موتور پروکسی) که همگی در یک کانتینر Docker اجرا می‌شوند. کافی است یک بار دیپلوی کنید.

---

## ۱) دیپلوی روی Railway (پیشنهادی)

### گام ۱ — آپلود پروژه در گیت‌هاب
1. یک ریپازیتوری **جدید** و **Private** در GitHub بسازید (مثلاً `titan`).
2. محتویات پوشه پروژه را push کنید:
   ```bash
   cd titan
   git init
   git add .
   git commit -m "TiTaN panel"
   git branch -M main
   git remote add origin https://github.com/USERNAME/titan.git
   git push -u origin main
   ```

### گام ۲ — ساخت پروژه در Railway
1. به [railway.app](https://railway.app) بروید و با حساب GitHub وارد شوید.
2. **New Project → Deploy from GitHub repo**.
3. ریپازیتوری `titan` را انتخاب کنید.
4. Railway فایل `Dockerfile` را **خودکار** شناسایی می‌کند و پروژه را build و اجرا می‌کند.
   - چیزی که اجرا می‌شود: `entrypoint.sh` → شروع Nginx + پنل
   - فایل `railway.json` هم برای تنظیمات سلامت (healthcheck روی `/health`) وجود دارد.

### گام ۳ — اتصال دیسک (Volume) — بسیار مهم ⚠️
دیتابیس SQLite در پوشه `/app/data` ذخیره می‌شود. بدون Volume، با هر دیپلوی مجدد **همه کاربران و تنظیمات پاک می‌شوند**:
1. در صفحه پروژه → تب **Volumes** → **New Volume**.
2. سرویس خود را انتخاب کنید و مسیر mount را `/app/data` بگذارید.
3. ذخیره کنید. از این به بعد داده‌ها دائمی می‌مانند.

**بدون Volume، کلید Reality هم هر بار از نو ساخته می‌شود.** لینک‌هایی که قبلاً منتشر کرده‌ای
`pbk=` قدیمی را دارند، پس صبح فردا همهٔ کلاینت‌های Reality هم‌زمان timeout می‌دهند و این دقیقاً
شبیه «ایرانسل بلاک کرده» به‌نظر می‌رسد، در حالی که فقط دیپلوی زده شده. اگر Volume نمی‌خواهی
بگذار، کلید را پین کن (گام ۴.۵ بند ۵) تا لینک‌ها پایدار بمانند؛ کاربر تازه‌ساخت هم همان لحظه
کلید درست را می‌گیرد.

### گام ۴ — دامنه و HTTPS
1. تب **Settings → Networking → Generate Domain**.
2. Railway یک دامنه `https://xxx.up.railway.app` با HTTPS (TLS در لبه Railway خاتمه می‌یابد) می‌دهد.
3. می‌توانید **Custom Domain** هم تنظیم کنید (در صورت استفاده از کلودفلر، حالت Proxy را روشن نکنید یا کانفیگ‌ها را بر اساس IP تمیز تنظیم کنید).

> ⚠️ **مهم — برای کار کردن کانفیگ‌ها:** بعد از ورود، در پنل به **تنظیمات → عمومی** بروید و «دامنه عمومی» را روی همان دامنه‌ای که Railway/Render به شما داده (مثلاً `xxx.up.railway.app`) قرار دهید و ذخیره کنید. تا وقتی این فیلد خالی باشد، لینک‌های ساخته‌شده به `127.0.0.1` اشاره می‌کنند و **هیچ کلاینتی وصل نمی‌شود**. سپس از **ابزارها → تست اتصال** برای بررسی Xray، کانفیگ و مسیر WebSocket استفاده کنید (وضعیت سرویس Xray روی داشبورد هم به‌صورت بنر نمایش داده می‌شود).

### گام ۴.۵ — فعال‌کردن TCP Proxy (برای اینکه کانفیگ‌ها روی همهٔ اپراتورها کار کنند) ⚠️

پیش‌فرض، لینک‌های پنل از لبهٔ HTTP ریلوی (`*.up.railway.app:443` + WebSocket) رد می‌شوند.
این شکل ترافیک در شبکهٔ موبایل ایران بر پایهٔ SNI/دامنه فیلتر می‌شود؛ نتیجه‌اش همین است که
روی همراه‌اول کار می‌کند و روی ایرانسل با «timeout» می‌خورد (پینگ جواب می‌دهد، چون ICMP است).
TCP Proxy ریلوی بایت خام را مستقیم به کانتینر می‌رساند: هیچ TLS میانی وجود ندارد و SNI‌ای که
DPI می‌بیند، SNI خود Reality است (مثلاً `www.microsoft.com`) — پس روی هر دو اپراتور کار می‌کند.

1. در ریلوی: **Services → your service → Settings → Networking → TCP Proxy → Enable**
2. در فیلد **Internal Port** عدد `10999` را بده (یا همان مقداری که `TITAN_RAW_ENTRY_PORT` می‌گذاری).
   ریلوی یک نشانی مثل `roundhouse.proxy.rlwy.net:11105` می‌سازد.
3. **Deploy** را بزن. پنل آن سه متغیر ریلوی را خودش می‌خواند:
   `RAILWAY_TCP_PROXY_DOMAIN`، `RAILWAY_TCP_PROXY_PORT`، `RAILWAY_TCP_APPLICATION_PORT`.
   پس از این، لینک‌های `TCP`/`Reality` خودکار به همین نشانی اشاره می‌کنند؛ لینک‌های WS/xHTTP/gRPC
   مثل قبل روی دامنهٔ HTTPS می‌مانند (آن‌ها به لبهٔ ریلوی نیاز دارند).
4. در پنل: **تنظیمات → TCP خام / Railway TCP Proxy** را باز کن. سه خط باید سبز باشد:
   نشانی شناسایی‌شده، «مسیریاب: روشن»، و «رفت‌وبرگشت: OK». اگر هشدار قرمز داد، همان متن
   می‌گوید کدام پورت را اشتباه زده‌ای. اگر نوشت «هیچ چیزی از پورت خام جواب نداد» و پورتی
   را نام برد، یعنی `PANEL_PORT` با پورت واقعی سرویس فرق دارد (مثلاً uvicorn را روی
   `--port 8080` بالا آورده‌ای ولی `PANEL_PORT=10000` مانده) — همان را ستون.
5. **کلید Reality را پین کن** (اگر Volume نداری، اجباری است). در همان کارت، فیلد
   «کلید خصوصی Reality»:
   1. روی VPS یا هر جایی که Xray داری: `xray x25519` → `Private key` را کپی کن.
      اگر Xray نداری، همین یک‌خطی پایتون هم یک کلید معتبر می‌دهد (پنل کلید عمومی را خودش
      از روی آن مشتق می‌کند):
      ```bash
      python3 -c "from cryptography.hazmat.primitives.asymmetric.x25519 import X25519PrivateKey as K;import base64;p=K.generate();print(base64.urlsafe_b64encode(p.private_bytes_raw()).decode().rstrip('='))"
      ```
   2. در پنل بچسبان و **پین کلید** را بزن. پنل کلید عمومی را خودش مشتق می‌کند (به باینری
      Xray نیازی نیست) و از آن لحظه همهٔ لینک‌های Reality همان `pbk=` را می‌برند.
   3. معادلِ متغیر محیطی: `TITAN_REALITY_PRIV=<همان ۴۳ کاراکتر base64url>` — روی Railway
      در **Variables** بگذار؛ بعد از آن هیچ دیپلوی‌ای لینک‌های مشتری را نمی‌کشد.
   «منبع کلید Reality» در همان کارت باید `pinned` بشود. تا وقتی `generated` است، کلید فقط
   تا عمر فایل دیتابیس زنده است.
6. **ضد DPI روی لینک‌های خام:** تنظیمات → «فعال‌سازی Fragment» را روشن کن.
   این پارامترها (`fp_len` / `fp_int`) از این نسخه روی لینک‌های `TCP`/`Reality` هم نوشته
   می‌شوند (قبلاً فقط WS می‌رفت)، و همین چیزی است که مشکل شناخته‌شدهٔ
   `failed to read client hello` را روی DPI موبایل ایران حل می‌کند. سمت کاربر هم باید
   v2rayNG/Sifray نسخهٔ جدید باشد (گزینهٔ Fragment در برنامه فعال شود).

چرا یک «مسیریاب» لازم است: ریلوی فقط **یک** پورت داخلی را فوروارد می‌کند، ولی اینباندهای خام
پنل روی پورت‌های جدا هستند (reality/tls/plain). پنل یک listener روی آن یک پورت می‌گذارد و
اتصال‌ها را با بایت اول جدا می‌کند: `GET/POST…` → پنل، `ClientHello` با SNI دِکوی ری‌لیتی →
inbound ری‌لیتی، بقیهٔ TLS → inbound TLS، و هر چیز دیگر → inbound ساده/شاتل‌ساکز.

| متغیر | پیش‌فرض | توضیح |
|---|---|---|
| `TITAN_RAW_ENTRY` | `auto` | `1` اجباری روشن، `0` خاموش؛ در حالت auto فقط وقتی لازم است که پورت منتشرشدنی نباشد (ریلوی) |
| `TITAN_RAW_ENTRY_PORT` | `10999` | پورت داخلی که TCP Proxy باید به آن بزند؛ اگر ریلوی `RAILWAY_TCP_APPLICATION_PORT` بدهد، پنل همان را انتخاب می‌کند |
| `TITAN_RAW_ENTRY_BIND` | `0.0.0.0` | آدرس bind مسیراب |
| `TITAN_TCP_PROXY_HOST` / `TITAN_TCP_PROXY_PORT` | خالی | برای Render/Fly/VPS یا وقتی ریلوی متغیرها را خالی تزریق می‌کند |

نکته‌ها:
- **هیستریا۲ و WireGuard روی ریلوی ممکن نیستند**: TCP Proxy فقط TCP است و UDP ندارد. این دو فقط روی VPS کار می‌کنند.
- ریلوی روی این مسیر **PROXY protocol نمی‌فرستد**، پس IP واقعی کلاینت داخل کانتینر دیده نمی‌شود؛
  «سقف دستگاه» و «IP مجاز» همان‌طور که در README آمده روی ریلوی اعمال‌نشدنی می‌مانند.
- اگر می‌خواهی نشانی خودت را داشته باشی: در DNS یک **CNAME** به `roundhouse.proxy.rlwy.net`
  بساز (مثلاً `raw.example.com`) و در پنل، `دامنه TCP Proxy` را روی `raw.example.com` بگذار؛
  **پورت همان پورت ریلوی می‌ماند** و اگر پشت Cloudflare است، ابر باید **DNS only** باشد.

### گام ۵ — ورود اولیه (بدون ثبت‌نام)
1. مرورگر را باز کنید و به `<دامنه>/login` بروید.
2. نام کاربری پیش‌فرض **`TiTaN`** را وارد کنید — رمز اولیه لازم نیست.
3. وارد داشبورد شوید.
4. ⚠️ **حتماً رمز اختصاصی تعیین کنید:** تنظیمات → امنیت → «رمز عبور جدید» (در حالت پیش‌فرض، فیلد «رمز عبور فعلی» را خالی بگذارید). بعد از این، ورود فقط با این رمز ممکن می‌شود.
5. در بخش **کاربران** اولین کاربر/کانفیگ را بسازید و لینک/QR را در کلاینت وارد کنید.

### متغیرهای محیطی (اختیاری)
| متغیر | پیش‌فرض | توضیح |
|---|---|---|
| `PORT` | `8000` | پورت عمومی — Railway خودش تزریق می‌کند |
| `TITAN_DATA_DIR` | `/app/data` | محل دیتابیس |
| `PANEL_PORT` | `10000` | پورت داخلی پنل (نیازی به تغییر نیست) |
| `XRAY_BIN` | `/usr/local/bin/xray` | مسیر باینری Xray |

---

## ۲) دیپلوی روی Render

1. پروژه را در GitHub داشته باشید (مانند بالا).
2. در [render.com](https://render.com) → **New → Web Service** → ریپازیتوری را وصل کنید.
3. Render فایل `render.yaml` را شناسایی می‌کند (runtime: docker).
4. یک **Persistent Disk** با مسیر `/app/data` بسازید (Render → Disks).
5. بعد از دیپلوی به `<دامنه>/setup` بروید.

---

## ۳) دیپلوی روی سرور شخصی (VPS با Docker)

```bash
# 1) کپی پروژه روی سرور
git clone https://github.com/USERNAME/titan.git && cd titan

# 2) build ایمیج
docker build -t titan .

# 3) اجرا (پورت 8000 + ولوم دائمی برای دیتا)
docker run -d \
  --name titan \
  -p 8000:8000 \
  -e PORT=8000 \
  -v "$(pwd)/data:/app/data" \
  --restart unless-stopped \
  titan

# 4) مرورگر → http://SERVER_IP:8000/setup
```

> برای HTTPS روی VPS، یک reverse proxy مثل Nginx/Caddy یا کلودفلر جلوی پورت 8000 بگذارید.

### اجرای مستقیم بدون Docker (تست محلی)
```bash
pip install -r requirements.txt
python3 -m app.main        # پنل روی http://localhost:10000
# یا
uvicorn app.main:app --host 0.0.0.0 --port 8000
# → http://localhost:8000/setup
```
> بدون Xray، پنل در حالت mock اجرا می‌شود (مدیریت کامل، بدون عبور ترافیک واقعی).

---

## ۴) به‌روزرسانی پنل

- **Railway / Render:** فقط push به گیت‌هاب کافی است؛ پلتفرم خودکار redeploy می‌کند (دیتا در Volume می‌ماند).
- **Docker روی VPS:**
  ```bash
  cd titan && git pull
  docker build -t titan . && docker rm -f titan
  docker run -d --name titan -p 8000:8000 -e PORT=8000 \
    -v "$(pwd)/data:/app/data" --restart unless-stopped titan
  ```

---

### عیب‌یابی: «کانفیگ روی یک اپراتور کار می‌کند و روی دیگری نه»

به‌ترتیب این سه را جدا کن، چون هر کدام مقصر متفاوتی دارد:

سه حالت را جدا کن، چون هر کدام مقصر دیگری دارد:

```bash
# ۱) آیا اصلاً TCP به پورت خام باز می‌شود؟ (روی لپ‌تاپی که با همان اینترنت موبایل وصل است)
nc -vz roundhouse.proxy.rlwy.net 11105
#   وصل نشد  -> مشکل از مسیر/اپراتور است؛ لینک را از TCP Proxy بده (گام ۴.۵)
#   وصل شد   -> برو به ۲
# ۲) آیا پنل از همان پورت جواب می‌دهد؟ (مسیریاب باید HTTP را به پنل برساند)
printf 'GET /health HTTP/1.1\r\nHost: probe\r\nConnection: close\r\n\r\n' \
  | nc roundhouse.proxy.rlwy.net 11105 | head -1
#   HTTP/1.1 200 OK -> مسیر سالم است؛ پس مشکل از خودِ کانفیگ است (۳)
# ۳) صحت کانفیگ و مسیر از دید سرور:
python scripts/verify_deploy.py https://<panel> --password '<admin>'
```

`verify_deploy.py` در بخش `[9]` همین‌ها را گزارش می‌کند: نشانی خام، وضعیت مسیریاب،
رفت‌وبرگشت از داخل سرور، و یک **TCP connect از شبکهٔ خودت** به پورت عمومی.
اگر `[9d]` با timeout شکست خورد، مشکل از مسیر اپراتور است؛ اگر PASS شد و باز کانفیگ
بالا نیامد، سراغ `security`/`sni`/`public_key` کانفیگ برو.

## ۵) نکات مهم و عیب‌یابی

| مشکل | راه‌حل |
|---|---|
| پنل بالا می‌آید ولی پروکسی وصل نمی‌شود | لینک از `<دامنه>/setup` و با host صحیح ساخته شده باشد؛ پروتکل انتقال (WS/XHTTP) را با کلاینت هماهنگ کنید |
| داده‌ها بعد از دیپلوی پاک می‌شوند | Volume با مسیر `/app/data` وصل نشده است |
| ریلوی «Application failed to respond» می‌دهد | Start Command عوض شده، یا `PANEL_PORT`/`PORT` ناهم‌تراز، یا Build قرمز — بخش ۷.۵ |
| `data dir ... is unusable` در لاگ | Volume روی `/app/data` mount نشده یا read-only است (پنل با دایرکتوری موقت بالا می‌آید) |
| صفحه `/setup` باز نمی‌شود و ریدایرکت به `/login` می‌شود | یعنی قبلاً ادمین ساخته شده؛ با همان رمز وارد شوید |
| Xray در لاگ «mock mode» نشان می‌دهد | باینری Xray در ایمیج نصب نشده؛ مطمئن شوید با Dockerfile رسمی دیپلوی کرده‌اید |
| HTTPS/دامنه سفارشی | روی Railway دامنه تولید کنید؛ کانفیگ‌ها به پورت 443 اشاره می‌کنند (TLS در لبه پلتفرم خاتمه می‌یابد) |

## ۶) امنیت
- رمز قوی انتخاب کنید (قفل خودکار بعد از ۸ تلاش ناموفق فعال است).
- ریپازیتوری را **Private** نگه دارید (فایل دیتابیس شامل اطلاعات حساس است و فقط در Volume نگهداری می‌شود).
- در صورت نشت لینک اشتراک، از دکمه «تغییر UUID» استفاده کنید.

---

## ۷) معماری چندسروره (چند لوکیشن) — Multi-node

هر سرویس Railway فقط **یک لوکیشن** می‌گیرد؛ برای داشتن چند کشور، یک پروژه با چند **سرویس** بسازید (یا چند پروژه). هر سرویس = یک نمونه کامل TiTaN که نقشش با متغیر محیطی تعیین می‌شود.

### نقش‌ها
| متغیر | مقدار | نقش |
|---|---|---|
| `TITAN_ROLE` | `main` (پیش‌فرض) | پنل اصلی = مغز: داشبورد، دیتابیس، مدیریت کاربران/سرورها |
| `TITAN_ROLE` | `node` | فقط پراکسی: Xray را برای کاربرانی که main بهش اختصاص داده اجرا می‌کند |

### متغیرهای مشترک (روی همه سرویس‌ها)
| متغیر | توضیح |
|---|---|
| `TITAN_NODE_SECRET` | یک رمز یکسان روی **همه** سرویس‌ها (main و node ها). بدون آن sync غیرفعال است. |
| `TITAN_MAIN_URL` | فقط روی node ها: آدرس عمومی پنل اصلی، مثلاً `https://panel.example.com` |
| `TITAN_NODE_SYNC_INTERVAL` | (اختیاری، فقط main) فاصله push مجدد کاربران به node ها به ثانیه؛ پیش‌فرض 60 |
| `TITAN_NODE_TOKEN` | (روش سریع) توکن یک‌بارمصرفی که پنل برای هر node صادر می‌کند؛ جایگزین `TITAN_NODE_SECRET` برای آن node |
| `TITAN_NODE_URL` | (اختیاری، فقط node) آدرس عمومی خود node. اگر خالی باشد به‌طور خودکار از `RAILWAY_PUBLIC_DOMAIN` خوانده می‌شود |

### چیدمان نمونه روی Railway
```
پروژه TiTaN
├── سرویس panel   → US West    → TITAN_ROLE=main,  TITAN_NODE_SECRET=XXXX
├── سرویس node-us → US East    → TITAN_ROLE=node,  TITAN_NODE_SECRET=XXXX, TITAN_MAIN_URL=https://panel.example.com
├── سرویس node-eu → EU West    → TITAN_ROLE=node,  ... (همانند بالا)
└── سرویس node-sg → Singapore  → TITAN_ROLE=node,  ...
```

### راه‌اندازی
1. پنل اصلی را مثل قبل دیپلوی کنید و `TITAN_ROLE=main` + `TITAN_NODE_SECRET` بدهید.
2. هر node را دیپلوی کنید: `TITAN_ROLE=node` + `TITAN_NODE_SECRET` + `TITAN_MAIN_URL`.
3. به هر node یک **دامنه عمومی** بدهید (`node-us.example.com`, ...).
4. در داشبورد اصلی → «سرورها» → «افزودن سرور»: هر node را با نام و **آدرس دامنه‌اش** ثبت کنید.
   - آدرس را به شکل `https://node-us.example.com` وارد کنید (پورت سفارشی هم مجاز است: `https://node-us.example.com:8443`).
   - بعد از ثبت، پنل یک **توکن اختصاصی** برای همان node صادر می‌کند و در یک پنجره نمایش می‌دهد؛ همان ۳ متغیر (`TITAN_ROLE` / `TITAN_MAIN_URL` / `TITAN_NODE_TOKEN`) را روی سرویس node تنظیم کنید تا کاربران به آن push شوند.
5. موقع ساخت کاربر/کانفیگ، از لیست «سرور» یکی از لوکیشن‌ها را انتخاب کنید.
6. در کارت هر سرور، وضعیت همگام‌سازی (`کاربر روی نود / کاربر موردانتظار`) دیده می‌شود؛ با دکمه **«همگام‌سازی فوری»** می‌توانید بدون انتظار، کاربران را همان لحظه به node بفرستید.

### راه‌اندازی سریع نود (پیشنهادی — بدون رمز مشترک)

برای کسانی که پروژه را fork کرده‌اند، اضافه‌کردن نود بدون کپی‌کردن دستی دامنه و بدون ردوبدل رمز مشترک ممکن است:

1. در پنل اصلی → **سرورها** → دکمه **«راه‌اندازی سریع نود»** را بزنید و یک نام بدهید.
2. یک **توکن یک‌بارمصرف** می‌گیرید. این ۳ متغیر را در سرویس جدید Railway تنظیم کنید:
   ```env
   TITAN_ROLE=node
   TITAN_MAIN_URL=https://panel.example.com
   TITAN_NODE_TOKEN=<توکن-دریافتی>
   ```
3. برای سرویس نود یک **دامنه عمومی (Public Domain)** در Railway بسازید — نود آدرس خودش را خودکار از `RAILWAY_PUBLIC_DOMAIN` می‌خواند (یا با `TITAN_NODE_URL` صریح بدهید).
4. نود چند ثانیه بعد از بالا آمدن، با همان توکن **خودش را در پنل ثبت می‌کند**: main آدرسش را پر می‌کند و کشور/پرچم را خودکار تشخیص می‌دهد.
5. در همان ویزارد، دکمه **«بررسی ثبت نود»** را بزنید — به محض اینکه نود ثبت شد، آدرس و پرچمش همان‌جا نمایش داده می‌شود.

نکته‌ها:
- با این روش دیگر نیازی به `TITAN_NODE_SECRET` برای آن نود نیست؛ توکن همان نقش رمز مشترک را برای آن نود بازی می‌کند.
- توکن فقط یک‌بار در ویزارد نمایش داده می‌شود و در API عمومی/لیست سرورها هرگز برگشت داده نمی‌شود.
- اگر روش کلاسیک (یک رمز مشترک روی همه) را ترجیح می‌دهید، مراحل «راه‌اندازی» پایین را دنبال کنید.

### چطور کار می‌کند
- کاربر با لوکیشن `US` انتخاب می‌شود → main کانفیگش را به `node-us` می‌فرستد؛
- لینک کاربر به دامنه همان node اشاره می‌کند → ترافیک واقعاً از همان کشور عبور می‌کند؛
- node مصرف کاربر را هر ~۳۰ ثانیه به main گزارش می‌دهد؛
- main هر ۶۰ ثانیه (و بلافاصله بعد از هر تغییر) کاربران را به node ها push می‌کند.

### نکات
- **رمز مشترک** باید روی همه یکسان باشد؛ اگر فراموش/ناهماهنگ باشد sync با خطای 401 رد می‌شود.
- کاربران node در دیتابیس خود node ذخیره می‌شوند؛ منبع حقیقت همیشه **main** است.
- پنل اصلی فقط کاربرانِ سرور لوکال خودش را proxy می‌کند؛ بقیه روی node خودشان هستند.
- Railway برای پراکسی پرترافیک ایده‌آل نیست؛ برای حجم بالا VPS اختصاصی (Hetzner/DigitalOcean/Vultr) پیشنهاد می‌شود، ولی روش راه‌اندازی یکی است.

---

## ۷.۵) ارور «Application failed to respond» در ریلوی — چک‌لیست

این متن پیش‌فرض ریلوی است وقتی **لبهٔ HTTP به پورت سرویس تو نرسد**؛ یعنی پنل یا
بالا نیامده یا روی پورتی نشسته که ریلوی به آن ترافیک نمی‌فرستد. به‌ترتیب چک کن:

1. **بیلد (Build) موفق بوده؟** اگر Build قرمز است، هیچ لاگی از اپ نیست و همان
   صفحهٔ عمومی را می‌بینی. لاگ Build را بخوان (معمولاً نصب `requirements.txt` یا دانلود Xray).
2. **Start Command** سرویس باید `bash /app/entrypoint.sh` باشد (همان چیزی که
   `railway.json` و `Procfile` می‌گذارند). اگر در تنظیمات سرویس دستی چیزی زده‌ای
   (مثلاً `python3 -m app.main` یا `uvicorn app.main:app`), nginx حذف می‌شود و
   ترافیک WS/لبهٔ HTTPS از بین می‌رود. از این نسخه، اگر در آن حالت `PORT` هم تزریق شده باشد
   خود پنل پورت را می‌گیرد و فوروارد می‌کند، پس پنل بالا می‌آید — ولی برای مسیر WS،
   همان Start Command استاندارد را بگذار.
3. **متغیرهای `PORT` و `PANEL_PORT` را دستی ست نکن.** ریلوی `PORT` خودش تزریق می‌کند؛
   اگر `PANEL_PORT` را روی عدد دیگری گذاشته باشی، nginx با entrypoint هم‌تراز می‌شود
   (این خودکار انجام می‌شود) ولی اگر **هر دو برابر** شوند، entrypoint پنل را یک پورت
   جابه‌جا می‌کند. خط‌های `[entrypoint] routing: ...` در Deploy log را نگاه کن: باید سه عدد
   (PORT / PANEL_PORT / RAW_ENTRY_PORT) را با چیزی که در ریلوی زده‌ای یکی ببینی.
4. **Volume**: مسیر mount باید دقیقاً `/app/data` باشد. اگر مسیر اشتباه یا read-only باشد،
   از این نسخه پنل باز هم بالا می‌آید و در لاگ می‌گوید
   `data dir /app/data is unusable ... falling back to /tmp/titan-data-xxxx` — یعنی
   پنل زنده است اما داده‌ها بعد از ری‌استارت می‌روند؛ Volume را درست کن.
5. **`GET /health` را مستقیم بزن** (روی همان دامنهٔ ریلوی). اگر JSON داد ولی داشبورد باز
   نشد، مشکل از کش/.asset است؛ اگر 502 داد، یعنی encore پورت: لاگ را بفرست.
6. **`raw_tcp` در پاسخ `/health`**: `true` یعنی پنل پورت عمومی را خودش گرفته (چون
   چیزی آنجا نبود). این **نشانهٔ تنظیم نادرست** است، نه ویژگی: لاگ هشدار
   `nothing served PORT=... so the panel took it over` را می‌گوید.

نکته: این ارور ربطی به TCP Proxy و Reality ندارد؛ آن‌ها روی پورت جدا (TCP Proxy) کار
می‌کنند و خرابی‌شان به شکل timeout کلاینت خودش را نشان می‌دهد، نه این صفحه.

## ۸) TCP Proxy (ترنسپورت TCP خام)

علاوه بر WS/XHTTP/gRPC، می‌توانید برای VLESS/VMess/Trojan از ترنسپورت **TCP** استفاده کنید.
TCP خام روی پورت‌های جداگانه و مستقیم سرو می‌شود (Xray خودش سوکت را نگه می‌دارد؛ TLS/Reality در خود Xray خاتمه می‌یابد، نه در nginx).

### پورت‌ها (پیش‌فرض، با متغیر محیطی قابل تغییر)
| پروتکل | امنیت | پورت | متغیر |
|---|---|---|---|
| VLESS | none | 10007 | `XRAY_TCP_VLESS_PORT` |
| VLESS | tls | 10008 | `XRAY_TCP_VLESS_TLS_PORT` |
| VLESS | reality | 10009 | `XRAY_TCP_VLESS_REALITY_PORT` |
| VMess | none | 10010 | `XRAY_TCP_VMESS_PORT` |
| VMess | tls | 10011 | `XRAY_TCP_VMESS_TLS_PORT` |
| Trojan | tls | 10012 | `XRAY_TCP_TROJAN_PORT` |

### حالت‌ها
- **None (خام):** بدون رمزنگاری — برای تست یا شبکه‌های داخلی. لینک به پورت 10007/10010 اشاره می‌کند.
- **TLS:** Xray خودش TLS را ارائه می‌دهد. نیاز به گواهی دارد:
  - `TITAN_TLS_CERT` → مسیر فایل `fullchain.pem` داخل کانتینر
  - `TITAN_TLS_KEY` → مسیر فایل `privatekey.pem`
  - اگر گواهی تنظیم نشده باشد، اینباند TLS ساخته نمی‌شود (در لاگ هشدار داده می‌شود).
- **Reality (فقط VLESS):** ضد فیلترینگ مدرن. جفت‌کلید x25519 یک‌بار با `xray x25519` تولید و در دیتابیس ذخیره می‌شود. قابل تنظیم:
  - پین کردن کلید: `TITAN_REALITY_PRIV` یا فیلد «کلید خصوصی Reality» در کارت TCP خام (`POST /api/reality/key`).
    بدون Volume این تنها راهی است که لینک‌های Reality بعد از redeploy زنده می‌مانند؛
    کلید خصوصی هیچ‌وقت از API بیرون داده نمی‌شود و در لاگ رویدادها هم نوشته نمی‌شود.
  - یک نکتهٔ واقعی: روی IP مشترک ابری، ایرانسل/همراه‌اول بعد از مدتی **خود IP** را بلاک می‌کنند
    (گزارش‌های Xray: ~۱ روز / ~۱۰۰ گیگ با SNIهای محبوب). هیچ تنظیم پنلی این را تضمین نمی‌کند؛
    چاره‌ها: دامنهٔ خودت روی IP تمیز (A/CNAME)، `dest` با دامنهٔ کم‌مخاطب، و fragment.
  - `TITAN_REALITY_DEST` (پیش‌فرض `1.1.1.1:443`) — مقصدی که وانمود می‌کند
  - `TITAN_REALITY_SNI` (پیش‌فرض `www.microsoft.com`) — دامنه‌ای که نمایش می‌دهد

### باز کردن پورت‌ها
- **VPS:** این ۶ پورت TCP را در فایروال باز کنید (Xray مستقیماً روی `0.0.0.0` گوش می‌دهد).
- **Railway/Render:** برای هر پورت TCP که استفاده می‌کنید یک **TCP Proxy** بسازید (پلتفرم پورت عمومی → پورت داخلی سرویس را map می‌کند). لینک‌ها به پورت داخلی اشاره می‌کنند؛ در صورت نیاز با متغیرهای بالا پورت عمومی را با پورت داخلی هم‌تراز کنید.

### انتخاب در پنل
هنگام ساخت کاربر/کانفیگ، در فیلد «انتقال» گزینه `TCP` را انتخاب کنید و در «امنیت» یکی از `TLS` / `None` / `Reality` را بزنید. پنل به‌طور خودکار پورت درست را در لینک قرار می‌دهد.

---

## ۹) Hysteria2، HTTPUpgrade و Fallback (فاز ۱ — سرعت و ضد-فیلترینگ)

### Hysteria2 (پیشنهادی برای سرعت بالا)
Hysteria2 روی **QUIC/UDP** کار می‌کند و روی شبکه‌های پرتاخیر/ناپایدار (موبایل، اینترنت ایران) سرعت و پینگ بهتری نسبت به TCP می‌دهد. از داخل UI: پروتکل `HYSTERIA2` را انتخاب کنید — لینک `hysteria2://` تولید می‌شود.

| متغیر | پیش‌فرض | توضیح |
|---|---|---|
| `XRAY_HY2_PORT` | `443` | پورت UDP که اینباند Hysteria2 روی آن گوش می‌دهد |
| `TITAN_TLS_CERT` / `TITAN_TLS_KEY` | — | **الزامی**؛ Hysteria2 خودش TLS (ALPN `h3`) را خاتمه می‌دهد |
| `TITAN_HY2_OBFS` | خالی (خاموش) | رمز Salamander؛ با ست‌شدن، لینک `obfs=salamander` می‌گیرد و ترافیک UDP ماسک می‌شود (نیاز به Xray-core جدید) |
| `TITAN_HY2_MASQUERADE_URL` | خالی (خاموش) | Masquerade برای پراب‌های بدون احراز: پاسخ را به یک سایت واقعی proxy می‌کند |

نکات:
- پورت **UDP** 443 را باز کنید. در Docker: `-p 443:443/udp`. روی Railway بررسی کنید که سرویس UDP را expose کند (در غیر این صورت از VPS استفاده کنید).
- Hysteria2 جدا از TCP 443 کار می‌کند؛ با Fallback زیر تداخلی ندارد (UDP ≠ TCP).
- کلاینت‌ها: v2rayN، NekoBox، sing-box، hysteria CLI (آدرس `hysteria2://...` را مستقیم import کنید).

### HTTPUpgrade (ترنسپورت سبک روی CDN)
ترنسپورت جدید `HTTPUPGRADE` (شبیه XHTTP ولی سبک‌تر) برای VLESS و VMess اضافه شده. از داخل UI در فیلد «انتقال» انتخاب کنید.
- مسیر: `/hup` → اینباند داخلی پورت `10013` (`XRAY_HTTPUPGRADE_PORT`)
- مثل WS از پشت nginx/CDN سرو می‌شود؛ به پورت عمومی معمولی (443) وصل می‌شود.
- nginx از قبل مسیر `/hup` را به `127.0.0.1:10013` فوروارد می‌کند.

### Fallback — همه روی یک پورت
با `TITAN_FALLBACK_PORT` می‌توانید **VLESS(TCP+TLS) + سه مسیر WebSocket** را روی یک پورت واحد سرو کنید (فقط اینباند VLESS+TLS ساخته می‌شود و مسیرهای WS را با sniffing به اینباندهای داخلی می‌فرستد).

| متغیر | پیش‌فرض | توضیح |
|---|---|---|
| `TITAN_FALLBACK_PORT` | `0` (خاموش) | پورت تک‌ورودی. روی VPS/Docker روی `443` بگذارید؛ روی Railway پورت 443 در اختیار HTTPS است، پس یک پورت TCP-proxy دیگر (مثلاً 8443) انتخاب کنید |
| `TITAN_TLS_CERT` / `TITAN_TLS_KEY` | — | **الزامی** (اینباند Fallback خودش TLS را خاتمه می‌دهد) |

رفتار:
- وقتی فعال است، کاربر `VLESS + TCP + TLS` به جای پورت 10008 روی پورت Fallback سرو می‌شود (لینک هم به همان پورت اشاره می‌کند).
- مسیرهای `/vl-ws`، `/vm-ws` و `/tr-ws` هم از همین پورت قابل دسترسی می‌شوند (اینباندهای WS داخلی با PROXY protocol متصل می‌شوند).
- بدون کاربر VLESS+TCP+TLS، اینباند Fallback ساخته نمی‌شود.

---

## ۱۰) فاز ۲ — مسیریابی خودکار، Shadowsocks 2022 و WireGuard

### مسیریابی خودکار نود (نزدیک‌ترین سرور)
هنگام ساخت کاربر، گزینه **«خودکار (نزدیک‌ترین سرور)»** (`node_id=0`) را انتخاب کنید:
- پنل به‌طور دوره‌ای (هر ~۴۵ ثانیه + هنگام باز شدن صفحه سرورها) پینگ هر نود را با پروب `/health` اندازه می‌گیرد.
- لینک/اشتراک کاربر خودکار به **سریع‌ترین نود آنلاین** اشاره می‌کند.
- کاربرِ خودکار به **همه نودهای فعال** sync می‌شود تا هر نودی بتواند سرویس بدهد.
- اگر هیچ نود ریموتی وجود نداشته باشد، کاربر خودکار روی سرور لوکال سرو می‌شود.

### Shadowsocks 2022 (ضد-شناسایی)
روش‌های جدید **2022-blake3** با PSK (شبیه WireGuard) اضافه شدند:
- هر کاربر روش مخصوص خودش را دارد (`ss_method`): سه روش 2022 (128/256/chacha20) + سه روش کلاسیک AEAD.
- PSK به‌صورت قطعی از `uuid` کاربر مشتق می‌شود (main و نودها همیشه هم‌کلیدند).
- روش‌های 2022 روی پورت جدا (`XRAY_SS_2022_PORT`، پیش‌فرض 10014) سرو می‌شوند؛ روش‌های کلاسیک روی 10006.
- در UI: با انتخاب پروتکل Shadowsocks، فیلد «روش رمزنگاری SS» ظاهر می‌شود.

### WireGuard / AmneziaWG (اختیاری — فقط VPS/Docker)
WireGuard به‌صورت **کاربر-فضا** (AmneziaWG) اجرا می‌شود؛ Xray آن را به‌عنوان اینباند سرور پشتیبانی نمی‌کند.

| متغیر | پیش‌فرض | توضیح |
|---|---|---|
| `TITAN_WG_PORT` | `51820` | پورت UDP سرور WireGuard |
| `TITAN_WG_SUBNET` | `10.200.0.0/24` | زیرشبکه داخلی برای کلاینت‌ها |
| `TITAN_WG_BIN` | `/usr/local/bin/amnezia-wg-go` | باینری کاربر-فضا (Dockerfile دانلودش می‌کند) |

الزامات:
- **VPS/Docker:** `--cap-add NET_ADMIN --device /dev/net/tun` + باز کردن پورت UDP 51820 + فعال‌کردن MASQUERADE (iptables) برای خروجی ترافیک کلاینت‌ها.
- **Railway/Render:** TUN در دسترس نیست؛ پنل این قابلیت را به‌صورت خودکار غیرفعال می‌کند (خطای مرگباری نمی‌دهد).

رفتار:
- کلید سرور هر نود یک‌بار تولید و ذخیره می‌شود؛ هر نود کلید عمومی خود را از طریق `/health` منتشر می‌کند و main آن را یاد می‌گیرد.
- برای هر کاربر WG یک جفت‌کلید + IP داخلی تولید می‌شود؛ کانفیگ کلاینت از `/api/users/<uid>/wireguard` و دانلود فایل `.conf` از `/api/users/<uid>/wireguard.conf`.
- لینک `wireguard://` (base64 کانفیگ) برای import در کلاینت‌ها (NekoBox/Streisand/sing-box).

---

<div align="center"><b>TiTaN</b> — fast, lightweight, magical ⚡</div>
